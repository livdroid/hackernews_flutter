name: flutter_hcknews
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^4.0.1
  http: ^0.12.0+4

dev_dependencies:
  flutter_test:
    sdk: flutter

  pedantic: 1.8.0+1
  mockito: ^4.1.1

flutter:

  uses-material-design: true